
[package]
name = "benchmark_rust"
version = "0.1.0"
edition = "2021"

[dependencies]
reqwest = { version = "0.11", features = ["blocking"] }
std_prelude = "0.2"
tokio = { version = "1", features = ["full"] }
chrono = "0.4"
